# Judge0 Configuration
# Auto-generated for UBIcode deployment

# Service port
PORT=2358

# Redis configuration for Judge0
REDIS_HOST=judge0-redis
REDIS_PORT=6379

# PostgreSQL configuration for Judge0
POSTGRES_HOST=judge0-db
POSTGRES_PORT=5432
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=judge0password

# CPU and Memory limit defaults
CPU_TIME_LIMIT=2.0
CPU_EXTRA_TIME=0.5
WALL_TIME_LIMIT=5.0
MEMORY_LIMIT=128000
STACK_LIMIT=64000
MAX_PROCESSES_AND_OR_THREADS=60
ENABLE_PER_PROCESS_AND_THREAD_TIME_LIMIT=true
ENABLE_PER_PROCESS_AND_THREAD_MEMORY_LIMIT=true
MAX_FILE_SIZE=1024

# Number of parallel workers
NUMBER_OF_WORKERS=2

# Maintenance and telemetry
ENABLE_COMPILER_OPTIONS=true
ALLOWED_LANGUAGES_FOR_COMPILER_OPTIONS=
ENABLE_WAIT_RESULT=true
WAIT_RESULT_POLL_INTERVAL=0.1
MAX_WAIT_RESULT_TIMEOUT=15.0
